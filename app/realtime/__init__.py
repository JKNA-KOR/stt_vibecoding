"""실시간(준실시간) 전사 계층.

**진짜 스트리밍 ASR 이 아니다.** 마이크에서 온 PCM 을 일정 길이로 잘라 기존 배치 엔진에
넣는 방식이며, 그래서 조각 경계에서 문맥이 끊긴다. 이 한계를 이름으로 감추지 않으려고
모듈 주석과 화면 양쪽에 같은 말을 적어 둔다 (Harness §4.3).

이렇게 한 이유는 엔진 교체 가능성(NFR-002)을 지키기 위해서다. 스트리밍 전용 경로를
따로 만들면 `STTEngine` 인터페이스 밖에 두 번째 전사 구현이 생기고, 그때부터 두 경로의
품질·설정·Provenance 가 갈라진다.
"""

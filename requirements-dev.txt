# 개발/테스트 전용 의존성. 운영 이미지에는 포함하지 않는다.
-r requirements.txt

pytest==8.4.2
pytest-asyncio==1.3.0
httpx==0.28.1                     # FastAPI TestClient 백엔드
ruff==0.14.5
pip-audit==2.9.0                  # Harness §40: 알려진 취약점 점검

# 샘플 녹취 생성 전용 (scripts/make_sample_recording.py). 서비스 런타임과 무관하며,
# 실행 시 외부 TTS 서비스에 대본 텍스트를 보낸다 — 폐쇄망에서는 설치·사용하지 않는다.
edge-tts==7.2.8
